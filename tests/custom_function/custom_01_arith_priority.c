int main(){
    int a = 7;
    int b = 3;
    int c = 2;
    putint(a + b * c); putch(10);
    putint((a + b) * c); putch(10);
    putint(a - b / c); putch(10);
    putint(a - b - c); putch(10);
    putint(a * b + c); putch(10);
    return 0;
}
